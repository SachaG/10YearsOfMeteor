---
---

<section>
  <img src="/stateofgraphql2022.png" alt="State of GraphQL 2022 developer survey" />
  <div>
    <slot />
  </div>
</section>

<style lang="scss">
  section {
    padding: var(--spacing);
    background: #454b58;
    color: #e8ecf5;
    display: flex;

    flex-direction: column;
    align-items: center;
    font-size: var(--step--1);
    gap: var(--halfSpacing);

    :global(a) {
      color: #feb8e6;
    }

    @media screen and (min-width: 900px) {
      flex-direction: row;
      margin-left: calc(-1 * var(--quadrupleSpacing));
      margin-right: calc(-1 * var(--quadrupleSpacing));
    }
  }

  img {
    width: 300px;
  }
</style>